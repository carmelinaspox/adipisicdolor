if (xDiff > 0) {
  // Assuming the code performs a specific action when xDiff is positive
  performActionForPositiveDiff(xDiff);
} else if (xDiff < 0) {
  // Handle the case when xDiff is negative
  performActionForNegativeDiff(xDiff);
} else {
  // Handle the case when xDiff is zero
  performActionForZeroDiff();
}

function performActionForPositiveDiff(diff) {
  // Optimized code for positive xDiff
}

function performActionForNegativeDiff(diff) {
  // Optimized code for negative xDiff
}

function performActionForZeroDiff() {
  // Optimized code for zero xDiff
}
